---
import PageLayout from '../layouts/PageLayout.astro';
import { getEntry } from 'astro:content';
import SocialLinks from '../components/SocialLinks.astro';
const { data } = await getEntry('home', 'home');
---

<style>
  h2 {
    @apply font-bold text-xl;
  }

  a {
    @apply text-blue-300 hover:text-blue-400;
  }
</style>
<PageLayout title="Home">
  <div class="grid place-items-center min-h-[500px] h-[calc(100vh-170px)] content-center">
    <div class="flex flex-col space-y-6 sm:flex-row sm:space-x-12 sm:space-y-0">
      <div class="shrink-0 w-64 mx-auto">
        <img class="rounded-full h-64 aspect-square object-cover" src=`/${data.imageUrl}` alt="Yuhao Ba" />
        <p class="text-sm text-center mt-3">
          Assistant Professor at
          <a href="https://lkyspp.nus.edu.sg/our-people/faculty/ba-yuhao" rel="noreferrer noopener" target="_blank">
            Lee Kuan Yew School of Public Policy
          </a>
        </p>
        <p class="text-sm text-center mb-5">National University of Singapore</p>
        <SocialLinks />
      </div>
      <div class="m-0">
        <h2>{data.title}</h2>
        <p class="mt-3">
          {data.description}
        </p>
      </div>
    </div>
  </div>
</PageLayout>
